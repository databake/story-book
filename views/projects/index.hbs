<h1>Projects</h1>
<div class="row">
  {{#each projects}}
  <div class="col s12 m4 l3">
    <div class="card">
      <div class="card-content center-align">
        <h5>{{ name }}</h5>
        <p>{{stripTags (truncate abstract 80)}}</p>
        <br />
       
        <!--div class="chip">
          <img src="{{ user.image }}" alt="" />
          <a href="/projects/user/{{ user._id }}">{{ user.displayName }}</a>
        </div-->
      </div>
        <div class="card-action center-align">
          <a href="/projects/{{ _id }}/stories" class="btn teal">User Stories ({{stories}})</a>
        </div>
      </div>
    </div>
    {{else}}
    <p>No projects to display</p>
    {{/each}}
  </div>
</div>
